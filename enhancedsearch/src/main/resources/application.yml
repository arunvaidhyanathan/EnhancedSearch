server:
  port: 8081 # Changed port from 8080 to 8081

spring:
  datasource:
    url: jdbc:postgresql://db.nrhsoabqeskybrznxfyi.supabase.co:5432/postgres
    username: postgres
    password: Sreeja1608!@
    driver-class-name: org.postgresql.Driver
  jpa:
    # Although we're primarily using JDBC, these might be useful if JPA is used elsewhere.
    hibernate:
      # Set ddl-auto to 'validate' or 'none' in production. 'update' can be risky.
      # 'create-drop' is suitable for testing. 'none' if schema managed externally.
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        # Optional: Show SQL statements generated by Hibernate (if JPA is used)
        show_sql: true
        format_sql: true
